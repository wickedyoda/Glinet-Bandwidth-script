# Setup Wizard

Run `/usr/local/sbin/glinet-vlan-qos-setup.sh` and follow the prompts:

1. Select **QoS** or **SQM** mode
2. Select your model or use **Auto-detect**
3. Choose **Persistent** or **Manual/Testing**
4. Assign priority to detected bridges
5. Review summary and confirm apply
